import React from 'react';
import { Route, Switch } from 'react-router-dom';

import 

const Routes = () => {
    return(
        <Switch>
            <Route path="/" exact component={}/>
            <Route path="/plans" exact component={}/>
        </Switch>
    )
}